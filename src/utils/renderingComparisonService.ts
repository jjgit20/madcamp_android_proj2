// export const arePropsEqual = (oldObject: any, newObject: any) => {
//   if (oldObject === newObject) {
//     return true;
//   }
//   const oldKeys = Object.keys(oldObject);
//   const newKeys = Object.keys(newObject);

//   if (oldKeys.length !== newKeys.length) {
//     return false;
//   }

//   return oldKeys.every(key => oldObject[key] === newObject[key]);
// };
